[project]
name = "telegram-time-bot"
version = "1.0.0"
description = "Simple Telegram time bot for Cloudflare Python Workers"
requires-python = ">=3.13"

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
